description = "Testing Orchestration"
silence = true

prompt = """
1. **Sync**: Check conversation history; skip reading `task.md` or `plan.md` if already seen.
2. **Setup**: Load `.env.test` if present; ensure `.venv` is active via `uv`.
3. **Standards**: Tests MUST use `pytest` in `/tests`, covering healthy, edge, and failure cases.
4. **Action**: Immediately run `uv run pytest -v`.
5. **Analyze**:
   - IF Fail: Log failure and fix attempt to `BUGS_LOG.md`.
   - IF Pass: Output ONLY "✅ All tests passed."
6. **Test Overview**:
   - Create `TEST_OVERVIEW.md` using the following style:
     ```markdown
     # 🧪 Test Overview: [Project Name]
     > **Total Tests:** [Count] | **Success Rate:** [Percentage]% | **Last Run:** [Date]

     ## 📊 Component Status
     | Area | Coverage | Integrity | Result |
     | :--- | :--- | :--- | :--- |
     | **[Component]** | 🟢 [Percentage]% | [Status] | `PASS` |

     ---
     ## 🔍 Deep Dive
     ### [Component] ([File])
     - **Scope**: [Description]
     - **Matrix**:
       - `Happy Path`: ✅ Verified
       - `Edge Cases`: ✅ Verified
       - `Error Handling`: ✅ Verified
     ```
"""
